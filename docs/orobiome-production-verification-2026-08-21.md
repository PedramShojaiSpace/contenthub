# Orobiome Production Verification — Aug. 21, 2026

The production collector is now live. A deliberately invalid JSON POST to `https://content.theurbanmonk.com/api/orobiome/funnel-event` returned `400 application/json`, confirming the request reached the new Express receiver rather than the prior static HTML fallback. No test event was inserted during that check.

The public Shopify page had already been updated to include the approved persistent 50/50 control versus hero-offer-clarity assignment and its anonymous tracking script. The rendered browser view showed the approved treatment with the existing $399 partner package, confirmed inclusions, and the unchanged native Shopify call to action.

The initial `/hub/orobiome-funnel` route displayed the Hub-core cross-bundle loading state because Orobiome had not been assigned to the split analytics bundle. The routing repair was built, tested, and published. The correct live route, [`/hub/analytics/orobiome-funnel`](https://content.theurbanmonk.com/hub/analytics/orobiome-funnel), now renders the protected dashboard and resolves its first-party summary successfully. It currently shows a valid zero state because no post-launch page-view events have been submitted after the receiver deployment; it clearly separates Control from A — Hero-offer clarity and displays page-view, 75%-scroll, CTA-click, cart-intent, purchase, and tracked-revenue measures.

# Native Unbounce Test-Page Repair Status — 2026-08-20

The published test page is **The Urban Monk - Interconnected Full Screening - Funnel LP copy 1**, page ID `9872b1ca-b228-46f8-b1fe-b3885c254663`, variant **E** (`326457903`), at `https://try.theurbanmonk.com/interconnected-lp-3/`.

Browser review of the duplicate editor confirmed that it contains both `Form 1` (the native Unbounce form) and `Custom HTML 2` (the legacy Klaviyo embed). This explains the two stacked forms visible on the public test URL. Owner confirmation was received to remove `Custom HTML 2` from this duplicate only. The original `/interconnected-lp/` page must remain unchanged.

The native form currently contains an optional `sms_consent` checkbox, but its full legal disclosure was entered as the field label and therefore renders poorly. The required layout repair is a short optional checkbox label with a separate readable disclosure text element. The test page must not be declared traffic-ready until the legacy embed is removed, the native consent presentation is corrected, the webhook maps `sms_consent`, and controlled email-only/SMS-consent validations pass.

During the approved removal, the first element-selection action completed but the browser artifact collector then encountered a transient extension-access error. A direct reload of the same variant editor is in progress; no successful delete or save has been confirmed yet.

Follow-up public-page review confirms the duplicate now visibly presents only the native form; the legacy Klaviyo form is no longer visible. The native form is positioned far too low in the registration panel and the lengthy consent disclosure is still rendered as a large field label. The next repair must move the native `Form 1` upward into the area below the registration headline and separate the compact checkbox label from the full legal disclosure.

The repaired public test page now displays a single native form with a centered email field, optional phone field, optional unticked `sms_consent` checkbox, contained submit button, and a separate linked Terms/Privacy disclosure inside the black registration panel. The remaining configuration requirement is to add the page-specific webhook mapping `sms_consent` → `sms_consent`, alongside its existing `email` → `email` and `phone_number` → `phone` mappings. The native receiver has already been implemented to treat a phone number without this explicit mapping/affirmative value as non-consensual SMS context only.

The page-specific webhook mapping was visually verified as `email` → `email`, `phone_number` → `phone`, and `sms_consent` → `sms_consent`. A controlled email-only test was submitted with `pedram+native-test-20260820a@theurbanmonk.com`, with phone blank and SMS consent unchecked. Unbounce recorded the test as a Variant E lead. The local lead record is ID `960001`, with `funnel_path = ko_klaviyo`, `phone = NULL`, `sms_consent = false`, `klaviyo_synced = true`, and `capi_lead_sent = true`; the browser completed the intended redirect to `/interconnected/thank-you-klaviyo`.

The active Klaviyo flow `YyFZPu` was visually verified as list-triggered with its Day 0 email set to Live; later follow-up nodes remain Draft. The owner approved `+13104208733` as the destination for the final explicit-consent SMS test. This number must be submitted only through the native test page with `sms_consent` selected, and the result must be checked against local lead consent state and the configured Interconnected SMS subscription handoff.

The final controlled native-form test has been filled with `pedram+native-sms-20260820a@theurbanmonk.com`, the owner-approved phone number, and the explicit consent checkbox selection. Submission and downstream verification are pending.

Unbounce’s lead record for the owner-submitted `pedram@theurbanmonk.com` test confirms the phone number and `SMS Updates (optional) = Yes`; its webhook integration also shows a successful delivery. However, the recipient local lead record (`960012`) stored `sms_consent = false`, despite the successful email/Klaviyo/CAPI handoff. The native receiver was updated to recognize the Classic Builder’s concrete checkbox-choice field (`sms_consent_yes`) in addition to the mapped parent field (`sms_consent`), while retaining the requirement for both an affirmative value and a phone number. Focused webhook regression coverage now passes 3/3. Production deployment and re-test remain required.
